[package]
name = "benchmarking"
version = "0.1.0"
edition = "2021"

[lib]
name = "benchmarking"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "benchmarks"
harness = false
