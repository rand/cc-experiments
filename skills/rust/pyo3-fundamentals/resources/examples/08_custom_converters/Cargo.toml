[package]
name = "custom_converters"
version = "0.1.0"
edition = "2021"

[lib]
name = "custom_converters"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
