[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "custom_converters"
version = "0.1.0"
description = "PyO3 example: custom_converters"
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3/extension-module"]
