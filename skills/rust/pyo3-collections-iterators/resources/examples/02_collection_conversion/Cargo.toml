[package]
name = "collection_conversion"
version = "0.1.0"
edition = "2021"

[lib]
name = "collection_conversion"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
