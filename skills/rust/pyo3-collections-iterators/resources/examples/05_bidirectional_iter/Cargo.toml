[package]
name = "bidirectional_iter"
version = "0.1.0"
edition = "2021"

[lib]
name = "bidirectional_iter"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
