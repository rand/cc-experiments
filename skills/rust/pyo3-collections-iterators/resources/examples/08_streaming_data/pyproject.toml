[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "streaming-data"
version = "0.1.0"
description = "Streaming large datasets with PyO3"
requires-python = ">=3.8"

[tool.maturin]
module-name = "streaming_data"
