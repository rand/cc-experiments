[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "parallel-iterator"
version = "0.1.0"
description = "Parallel iteration with Rayon with PyO3"
requires-python = ">=3.8"

[tool.maturin]
module-name = "parallel_iterator"
