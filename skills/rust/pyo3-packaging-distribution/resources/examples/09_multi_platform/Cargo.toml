[package]
name = "multi_platform"
version = "0.1.0"
edition = "2021"

[lib]
name = "multi_platform"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
