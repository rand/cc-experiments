[package]
name = "terminal_colors"
version = "0.1.0"
edition = "2021"

[lib]
name = "terminal_colors"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
regex = "1.10"
