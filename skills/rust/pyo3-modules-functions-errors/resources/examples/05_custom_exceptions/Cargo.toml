[package]
name = "custom_exceptions"
version = "0.1.0"
edition = "2021"

[lib]
name = "custom_exceptions"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
