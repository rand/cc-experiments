[package]
name = "error_hierarchy"
version = "0.1.0"
edition = "2021"

[lib]
name = "error_hierarchy"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
